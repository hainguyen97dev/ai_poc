# Dev/test-only dependencies — not needed at runtime, not baked into the
# Docker image (see .dockerignore). Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest==9.1.1
